name: Test Docker Action

on:
  push:
    branches: [ main ]

jobs:
  run-action:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run my Docker action
        uses: ./
        with:
          username: "Aristide"
